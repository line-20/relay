---
generated: 2026-09-17 21:38
branch: (start fresh off main — no live branch; the contracts worktree is idle)
item: contracts/handover-contract
phase: Relay 2.0 — versioned stage contracts
---

# Handover: checkpoint replication is live — next is the broader stage-contract work

## Where we are

Relay 2.0's first stage contract — the handover→continue **resume-state contract** — is now not just built and git-durable but **wired into the happy path**. As of PR #34 (merged, `8c77ec0`), a normal `/handover` (and therefore `/ship`) automatically emits the thread's resume state as a checkpoint and pushes it to `origin/main`, so a fresh clone on another machine resumes with no access to the original worktree. Cross-device resume — "continue any step on any device" — is live in normal use, not just possible.

## What just landed

- **PR #34 merged** (`8c77ec0`; commits `86a1c67` feat → `43b3882` fix → `daf56b9` review-doc). Slice 4 of `relay/briefs/handover-contract.md`.
- `/handover` Step 4a (`plugins/relay/commands/handover.md`, + generated `rlh.md`): emits the resume state and runs `relay_harvest.py apply` (with replication) **in the slice worktree**, before the handover push. The result JSON is written with the **Write tool, never shell-interpolated** (security). Runs with `--no-projection` (handover stays canonical) and `--no-board` (Step 4b owns the board). Step 6 cleans the worktree's `checkpoint.json`/`applied.json`.
- `scripts/relay_harvest.py`: `_replicate_checkpoint` reworked to the **temp-index-onto-freshly-fetched-tip** primitive — no working-tree/index/HEAD/local-branch mutation, no branch guard, runs from any checkout. This fixed two review 🔴s: a parallel-worktree-safe-commit violation and a **permanent non-fast-forward after the first handover** (Step 4b advances origin/main without advancing local main). New `apply --no-projection`/`--no-board` flags; `write_projection` param; recursive `_scan_provider_leak` (nested provider secrets rejected). WAL (`relay/harvest/*/results/`) gitignored.
- Review (3 blockers found, all fixed + re-reviewed clean): `relay/reviews/pr-34-2026-09-17.md`. Decision + review correction: `relay/decisions.md` slice-4 entry. Design: `docs/harvest-design.md` R1.12.
- Tests: **69 green** (`python3 scripts/tests/test_relay_harvest.py` 56 + `test_reflect_commands.py` 13; CI `python-tests` + `version-sync` both pass).

## In flight

None — PR #34 merged, tree clean. The `contracts` topic worktree is idle; a fresh `/next`/`/continue` re-baselines it.

## Next objective

The resume-state contract is done. The **broader Relay 2.0 goal** is *versioned handover contracts per stage* (see AI memory `relay-2.0-stage-contracts` and `relay/roadmap/`): derive per-stage contracts from the CastlesERP telemetry corpus. That corpus was noted stale (first analysis was Aug 2026) and the contract design hadn't started — so the real next step is likely **re-deriving/refreshing the stage-signature corpus** (cf. commit `1bd659b` "re-derive stage signatures against refreshed 859-span corpus") before designing the next stage's contract. Confirm the current corpus state first.

## Context you need (so you don't re-research)

- The checkpoint runtime is `scripts/relay_harvest.py`; the resume side is already wired (`continue.md`/`rlc.md` call `relay_harvest.py resume <slug>`), and now the write side is too (`handover.md` Step 4a). The emit/apply contract: `emit` (stdin JSON → WAL), `apply --slug <s> [--no-projection] [--no-board] [--no-replicate]` (WAL → checkpoint → temp-index push). Result schema + validation in `validate_result`/`_validate_resume_delta`.
- **Dogfooding caveat for the wiring:** the `/handover` command *file* now carries Step 4a, but the *installed* plugin command is whatever's published — so Step 4a first runs for real only after the plugin picks up this change. First real run is worth watching.
- Repo conventions: no `board.md`; `relay/briefs/` + `relay/roadmap/` gitignored; `relay/reviews`, `relay/decisions.md`, `relay/handover`, `relay/harvest/*/checkpoint.json` tracked, `relay/harvest/*/results/` gitignored. Tests run plainly with `python3` (no pytest). Durable state commits to `main` via the temp-index-from-FETCH_HEAD pattern (never a working-tree commit from a worktree) — `docs/conventions.md` "Parallel-worktree-safe commits".

## Start here

1. Read AI memory `relay-2.0-stage-contracts` and `relay/roadmap/` for the 2.0 plan and the telemetry-corpus state.
2. Check whether the stage-signature corpus is current (`docs/stage-signatures.md`; last re-derive was `1bd659b`) — decide if a refresh is the first slice.
3. If picking up a build slice, `/relay:next contracts/<next-slug>` — reuses the `contracts` worktree.

## Done when

The next stage contract's design is grounded in a current corpus, or the corpus refresh is scoped as its own slice. Scope edges: don't design contracts on a stale corpus; keep the checkpoint schema stable (it's now load-bearing for live cross-device resume).

## Open questions

- Is the CastlesERP telemetry corpus current enough to derive the next stage contract, or does it need refreshing first?
- Should `/persist` from this lap promote the temp-index-for-cross-checkout-commits pattern into guardrails/design docs, and add a release note for cross-device resume? (Offered at ship close, not yet run.)
