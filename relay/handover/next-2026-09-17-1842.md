---
generated: 2026-09-17 18:42
branch: (start fresh off main — no live branch; the contracts worktree is idle)
item: contracts/handover-contract
phase: Relay 2.0 — versioned stage contracts
---

# Handover: slice 3 (optional) — make the harvest checkpoint git-durable

## Where we are

Relay 2.0's first stage contract — the handover→continue **resume-state contract** — is built and shipped. The canonical resume object is `<root>/harvest/<slug>/checkpoint.json` (from the earlier harvest R1 work); this thread hardened its `resume_delta` with field-level validation at **both** ends:

- **Slice 1** (PR #31, merged `610f59c`): emit-time validation in `validate_result` (`scripts/relay_harvest.py`).
- **Slice 2** (PR #32, merged `6e83ad2`): read-time validation — `resume_context` fails closed on a malformed/legacy checkpoint; `discover_active` degrades per-item to a `checkpoint_status: valid|invalid` field.

The contract's substance is done. **Only slice 3 remains, and it is an optional stretch** — the brief marked it so, and nothing depends on it.

## What just landed

- `scripts/relay_harvest.py`: `_validate_resume_delta` (field-level shape, present-null rejected via `isinstance` guard) + `_checkpoint_resume_delta` (validated read helper, raises `HarvestError`). `resume_context` fails closed; `discover_active` degrades (narrow `except HarvestError`; flags an unparseable checkpoint file as `invalid`).
- `scripts/tests/test_relay_harvest.py`: 34 tests green (`python3 scripts/tests/test_relay_harvest.py`; CI `.github/workflows/python-tests.yml`).
- `relay/decisions.md`: the challenger ruling on slice 2's split (raise-in-resume / degrade-in-discover).
- Reviews: `relay/reviews/pr-31-*.md`, `relay/reviews/pr-32-*.md`.

## In flight

None — tree is clean, both PRs merged, branches deleted. The `contracts` topic worktree is on disk (`.claude/worktrees/contracts`) and idle; a fresh `/next` or `/continue` re-baselines it.

## Next objective

**Slice 3 (optional): checkpoint git-durability.** From the brief (`relay/briefs/handover-contract.md` → Slices) and `docs/harvest-design.md` R1.6 (line ~312): the checkpoint currently "lived on-disk in the worktree, resolvable but uncommitted — a hardening, not proven." Make the checkpoint recoverable from git so a pruned or foreign-machine worktree still resumes. **Acceptance:** after a lap, the checkpoint is recoverable from git without the original worktree.

## Context you need (so you don't re-research)

- `scripts/relay_harvest.py`: `apply_one` (~line 200) writes `checkpoint.json` via `_atomic_write` — currently a plain file write, **not** committed. `autosave` (~line 640) already exists and makes a local `wip:` commit of the worktree; slice 3 likely commits the checkpoint alongside or via a similar mechanism. Decide whether the checkpoint commits to the topic branch, to main, or to a dedicated ref.
- Durability model: R1.6 splits **local durability** (commit, offline-safe) from **remote durability** (push, opportunistic). Slice 3 is about the checkpoint surviving worktree loss — local commit is the floor; a `replicated` flag tracks remote.
- Autonomy is `challenge` (`relay.config.local.json`): a design fork here (which ref the checkpoint commits to) is an outlive-the-lap decision → dispatch the `challenger`, log to `relay/decisions.md`.
- Repo conventions: no `board.md` and `relay/briefs/` is gitignored (maintainer dogfooding scratch) — briefs live locally; `relay/reviews`, `relay/decisions.md`, `relay/handover` are tracked. Tests run plainly with `python3` (no pytest).

## Start here

1. Read `relay/briefs/handover-contract.md` (Slices + Open questions) and `docs/harvest-design.md` R1.5–R1.6.
2. `/relay:next handover-contract` (reuses the `contracts` worktree, re-baselines off main) — or decide slice 3 isn't worth it and close the thread.
3. Frame the "which ref does the checkpoint commit to" fork for the challenger before writing code.

## Done when

The checkpoint is recoverable from git after the worktree is gone (a test proves it), local-commit floor in place. Scope edges: do NOT build the remote-push/replication half beyond a `replicated` flag if present; do NOT touch the concurrency/runtime-supervisor layer.

## Open questions

- Is slice 3 worth doing at all now? The contract is trustworthy without it; git-durability guards only the pruned/foreign-machine-worktree case. Reasonable to defer or drop.
- Which ref does the checkpoint commit to — the topic branch, main (like board/handover), or a dedicated `refs/relay/checkpoints/*`? (challenger question.)
